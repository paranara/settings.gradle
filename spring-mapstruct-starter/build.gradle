apply from: rootProject.file("build-publish-mavencentral.gradle")

dependencies {
    implementation project(':spring-mapstruct-starter-core')
    implementation group: 'org.springframework.boot', name: 'spring-boot-starter', version: springBootVersion
    implementation group: 'org.springframework.boot', name: 'spring-boot-configuration-processor', version: springBootVersion
}
